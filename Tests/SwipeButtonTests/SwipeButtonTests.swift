    import XCTest
    @testable import SwipeButton

    final class SwipeButtonTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SwipeButton().text, "Hello, World!")
        }
    }
